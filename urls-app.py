from django.conf.urls import include, url
#from .views import NameView
#from modules.appname import api

urlpatterns = [
	#url(r'^api/tags/$', TagList.as_view(), name='api-tag-list'),
	#url(r'^some/url/$', NameView.as_view(), name='room-detail'),
]